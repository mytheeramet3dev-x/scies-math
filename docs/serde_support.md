# `serde_support` Module Documentation

Phase 5C — `serde` feature: Serialize / Deserialize for core types.

Enable via `Cargo.toml`:
```toml
[dependencies]
scies-math = { version = "0.2", features = ["serde"] }
```